# Week Ahead Chart - Tasks

- [x] Component scaffold + loading state
- [x] Data fetching (events / routines / defaults / one-time items / manual)
- [x] Compute counts per day with "leaves home" filter
- [x] SVG render (lines + dots + day labels)
- [x] Bulk event-items API consumption
- [ ] Cache results by week-start to avoid refetching when only the selected date inside the same week changes (deferred)
