import { AppShell } from "@/components/shared/AppShell";

export default function PathwiseStrategyLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <AppShell>{children}</AppShell>;
}
